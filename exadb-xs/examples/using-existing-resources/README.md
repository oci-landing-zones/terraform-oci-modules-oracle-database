# Using existing ExaDB-XS resources

This example supplies storage-vault and VM-cluster dependency maps without
creating either resource. It demonstrates the stable external dependency shape.
DB-home configuration is intentionally null until the required common-database
precondition update is released.
